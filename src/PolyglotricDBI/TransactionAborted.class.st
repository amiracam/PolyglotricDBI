Class {
	#name : #TransactionAborted,
	#superclass : #Exception,
	#category : #'PolyglotricDBI-Error handling'
}

{ #category : #accessing }
TransactionAborted >> defaultAction [
	"The default action taken if the exception is signaled."

	
]
